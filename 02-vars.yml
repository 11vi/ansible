 - name: Demo on variable
   hosts: all
   vars:
     URL: play.google.com
   tasks:
     - name: print a nariable
       debug:
         msg: "URL = {{URL}}"
     - name: print task variable
       debug:
         msg: "URL = {{URL}}"
       vars:
         URL: task.google.com

